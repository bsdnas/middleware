<%
	buildtime = middleware.call_sync('system.info')['buildtime']
	motd = middleware.call_sync('system.advanced.config')['motd']
%>\

	BSDnas — a community fork continuing TrueNAS CORE on current FreeBSD.
	Not affiliated with or endorsed by iXsystems, Inc.

	Released under the modified BSD license, with parts under LGPL-3.0.
	Inherited code remains copyright of its original authors, including
	iXsystems, Inc. and the FreeNAS Project.

	Documentation and issue tracker:
	https://bsdnas.com
% if motd:
${motd}
% endif
